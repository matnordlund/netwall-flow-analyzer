# NetWall Flow Analyzer – example environment variables
# The app is currently configured via CLI args; these names can be used
# by a process manager (e.g. systemd, Docker) or a thin wrapper script.

# Database (SQLAlchemy URL)
# DATABASE_URL=postgresql+psycopg://user:pass@localhost/netwall
# DATABASE_URL=sqlite:///./data/netwall.db

# HTTP server
# WEB_HOST=0.0.0.0
# WEB_PORT=8080

# UDP syslog receiver
# SYSLOG_HOST=0.0.0.0
# SYSLOG_PORT=5514

# Optional: serve built frontend from backend
# SERVE_FRONTEND=true
# FRONTEND_DIR=./frontend/dist

# Logging
# LOG_LEVEL=info

# Year mode and classification (see --year-mode and --classification-precedence)
# YEAR_MODE=current
# CLASSIFICATION_PRECEDENCE=zone_first
